fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.17
httpx==0.27.2
pydantic==2.9.2

# Test-only — only needed by pytest; safe to include in the prod image
# (small footprint), avoids a separate requirements-dev split.
pytest==8.3.4
coverage==7.6.10
